{
  "name": "video-pack",
  "version": "0.1.0",
  "description": "Agent-guided production system for turning narrated scripts into reviewed, editable video packs.",
  "type": "module",
  "bin": {
    "video-pack": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "cli": "node dist/index.js",
    "demo:status": "node dist/index.js status --project ./examples/bees-pollination-explainer",
    "demo:mock": "node dist/index.js validate --project ./examples/bees-pollination-explainer && node dist/index.js analyze --project ./examples/bees-pollination-explainer --force && node dist/index.js plan --project ./examples/bees-pollination-explainer --force && node dist/index.js proposal --project ./examples/bees-pollination-explainer --force && node dist/index.js prepare --project ./examples/bees-pollination-explainer --force && node dist/index.js visual-events --project ./examples/bees-pollination-explainer --force && node dist/index.js prompts --project ./examples/bees-pollination-explainer --force && node dist/index.js preview --project ./examples/bees-pollination-explainer --count 5 --provider mock --force && node dist/index.js generate-images --project ./examples/bees-pollination-explainer --provider mock --force && node dist/index.js visual-assets --project ./examples/bees-pollination-explainer --provider mock --force && node dist/index.js generate-thumbnails --project ./examples/bees-pollination-explainer --provider mock --force && node dist/index.js package --project ./examples/bees-pollination-explainer --draft --force && node dist/index.js remotion --project ./examples/bees-pollination-explainer --force && node dist/index.js board --project ./examples/bees-pollination-explainer --force && node dist/index.js status --project ./examples/bees-pollination-explainer"
  },
  "dependencies": {
    "archiver": "^8.0.0",
    "commander": "^12.1.0",
    "csv-stringify": "^6.5.1",
    "dotenv": "^16.4.5",
    "fs-extra": "^11.2.0",
    "music-metadata": "^11.13.0",
    "yaml": "^2.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.14.9",
    "typescript": "^5.5.3",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20"
  }
}
