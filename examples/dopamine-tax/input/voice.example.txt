Optional voiceover placeholder.

To test real audio:

1. Record the script as `input/voice.mp3`.
2. Update `project.yml`:

```yaml
input:
  audio_file: "./input/voice.mp3"
```

3. Run:

```bash
video-pack audio-info --project .
video-pack transcribe --project . --provider openai
```
